const Sign_Up = "/";
const Home = "/home";
const Sign_In = "/sign_in";
const Help_Center = "/help_center";
const Review = "/review";

const Path = {
  Sign_Up,
  Home,
  Sign_In,
  Help_Center,
  Review,
};

export default Path;
